/*
Escreva um programa para converter de dólar para real e exibir para o usuário a
resposta final.
*/

const dolar = 5.25;

var num = prompt('Valor em $: ');

var real = parseFloat(num) * dolar;

real = Math.floor(var1 * 100) / 100;

console.clear()
console.log(decimal('R$', real));
